# Example environment variables for local development
# If you run a Strapi backend, set VITE_STRAPI_URL to it, e.g.:
# VITE_STRAPI_URL=http://localhost:1337

# If not set, the app will fall back to sample recipes in src/lib/sample-recipes.ts

# Access Gate - Secret key stored in env only (never in source code)
# Set this to your 11-char secret key. On Railway, set this in env vars UI.
VITE_ACCESS_SECRET=